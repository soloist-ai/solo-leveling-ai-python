apiVersion: v1
kind: Service
metadata:
  name: solo-leveling-ai-python
spec:
  selector:
    app: solo-leveling-ai-python
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      name: http