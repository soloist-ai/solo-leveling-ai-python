# =============================================================================
# Solo Leveling AI - Production Overrides
# =============================================================================

app:
  environment: production
  debug: false

features:
  use_schema_registry: false

kafka:
  bootstrap_servers: kafka-service:9092
  consumer:
    session_timeout_ms: 45000
  producer:
    retries: 5

schema_registry:
  url: http://schema-registry-service:8081

logging:
  level: ERROR
  handlers:
    console:
      level: ERROR
    file:
      enabled: true
      level: ERROR
